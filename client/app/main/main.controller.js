'use strict';

angular.module('authWithNodeApp')
  .controller('MainCtrl', function ($scope, $http) {
    var vm = this;
    activate();

    function activate() {
      // Initialize the view-model here.
    }

  });
